<!-- User Index Page -->
<div class="user-page">

	<div class="container">
		
		<h1>Users#index</h1>
		<p>Users in the database</p>
		
		<ul>
			<% @users.each do |u| %>
				<li>
					<strong> <%= link_to "#{u.name}", user_path(u) %></strong>
				</li>
			<% end %>
		</ul>
	</div>
		

</div>